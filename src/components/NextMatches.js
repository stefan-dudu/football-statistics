import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NextMatches = () => {
  return (
    <View>
      <Text>NextMatches</Text>
    </View>
  );
};

export default NextMatches;

const styles = StyleSheet.create({});
