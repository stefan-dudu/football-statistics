import { StyleSheet, Text, View } from "react-native";
import React from "react";

const StatsTransfers = () => {
  return (
    <View>
      <Text>StatsTransfers</Text>
    </View>
  );
};

export default StatsTransfers;

const styles = StyleSheet.create({});
